import { styled } from '@config/stitches.config'

export const Form = styled('form', {
  display: 'flex',
  flexDirection: 'column',
  gap: '.75rem',

  button: {
    width: '100%',
    marginTop: '1.5rem',
  },
})
